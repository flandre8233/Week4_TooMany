using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TankReloader : TankMemberControlParts
{
    // Update is called once per frame
    void Update()
    {
        if (TargetGamePad == null)
        {
            return;
        }


    }
}
